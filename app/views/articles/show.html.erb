<h1><%= @article.title %></h1>
<p>
  Tags:
  <% @article.tags.each do |tag| %>
    <%= link_to tag.name, tag_path(tag) %>
  <% end %>
</p>
<br />
<h3>Article Body</h3>
<p><%= @article.body %></p>
<p> Remember to show attachments! </p>
<p><%= image_tag @article.image_list %></p>


<h3>Comments (<%= @article.comments.size %>)</h3>
<p id="existing_comments"><%= render partial: 'articles/comment', collection: @article.comments %></p>
<br />
<p id="add_a_comment"><%= render partial: 'comments/form' %></p>
<br />

<%= link_to "<< Back to Articles List", articles_path %>
<%= link_to "Delete", article_path(@article), method: :delete, data: {confirm: "Are you sure you want to delete this article?"} %>
<%= link_to "Edit", edit_article_path(@article) %>